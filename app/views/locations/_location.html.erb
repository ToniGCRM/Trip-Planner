<h1><%= @location.id ? 'Edit' : 'New' %></hi>
<br/>
<div class = 'form'>
  <%= form_for [@trip, @location] do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, required: true  %>
    <%= f.submit %>
  <% end %>
</div>